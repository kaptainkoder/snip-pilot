<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    Hardened Runtime entitlements for Developer ID (direct distribution) signing.
    These are required by Electron under the hardened runtime; they are NOT the
    App Sandbox (that is a separate, larger effort tracked in NEXT_VERSION.md §6).
    This file is inert during the current ad-hoc local build and only takes effect
    once you sign with a real "Developer ID Application" certificate.
  -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
</dict>
</plist>
